name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - master

jobs:

  checkout:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
